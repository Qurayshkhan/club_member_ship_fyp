import React, { useEffect } from "react";
import { Link } from "@inertiajs/react";
let MasterLayout = ({ children, images }) => {
    const {
        logo,
        data_set_bg_one,
        data_set_bg_two,
        className_1,
        className_2,
        className_3,
        className_4,
        className_5,
    } = images;

    return <></>;
};

export default MasterLayout;
